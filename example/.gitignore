# Files and directories created by pub / Flutter tooling.
.dart_tool/
.packages
build/
pubspec.lock

.flutter-plugins
.flutter-plugins-dependencies
.metadata

# Platform runner scaffolding is NOT committed — regenerate it locally with
# `flutter create .` (see README.md). Keeps the published pub archive small.
android/
ios/
linux/
macos/
web/
windows/
